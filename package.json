{
  "name": "rocketcrab-nova",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n nova,runtime -c blue,magenta \"npm run dev --workspace @rocketcrab/nova\" \"npm run dev --workspace @rocketcrab/runtime\"",
    "build": "npm run build --workspaces --if-present",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:watch": "concurrently -k -n nova,runtime -c blue,magenta \"npm run test:watch --workspace @rocketcrab/nova\" \"npm run test:watch --workspace @rocketcrab/runtime\"",
    "test:e2e": "playwright test",
    "bundle:size": "node scripts/bundle-size.mjs",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "concurrently": "10.0.4",
    "lefthook": "2.1.10",
    "oxfmt": "0.61.0",
    "oxlint": "1.76.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@11.16.0"
}
