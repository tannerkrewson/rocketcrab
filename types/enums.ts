export enum LobbyStatus {
    loading = "loading",
    lobby = "lobby",
    ingame = "ingame",
}

export enum GameStatus {
    loading = "loading",
    inprogress = "inprogress",
    waitingforhost = "waitingforhost",
    error = "error",
}
