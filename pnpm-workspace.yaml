packages:
  - "apps/*"
  - "packages/*"

# Allow postinstall scripts for packages that need them (pnpm 11 defaults to
# blocking build scripts until explicitly approved).
allowBuilds:
  lefthook: true
  playwright: true
  esbuild: true
