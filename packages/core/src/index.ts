/**
 * @rocketcrab/core
 *
 * Party, authority, state and simulation engines, plus the transport-neutral
 * interface implemented by InMemoryTransport (U5) and TrysteroTransport (P1),
 * and the shared game-repository contract (U2). Fully populated by later
 * issues.
 */
export * from "./repository";

export const packageName = "@rocketcrab/core";
