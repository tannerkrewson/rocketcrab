/**
 * @rocketcrab/protocol
 *
 * Shared Zod schemas, protocol types, and versioning for all cross-context
 * and peer messages. Fully populated by F6 — Define versioned protocol schemas.
 */
export const packageName = "@rocketcrab/protocol";
